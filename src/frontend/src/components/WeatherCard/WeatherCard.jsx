import {Card, CardHeader} from '@mui/material';

const WeatherCard = () => {
  return <>
    <Card sx={{maxWidth: 345}} style={{backgroundColor: '#757DE8'}}>
      <CardHeader>The Weather</CardHeader>
      <p>Hello There</p>
    </Card>
  </>;
};

export default WeatherCard;
